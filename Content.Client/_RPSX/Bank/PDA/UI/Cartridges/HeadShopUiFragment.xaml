<cartridges:HeadShopUiFragment xmlns="https://spacestation14.io"
                               xmlns:cartridges="clr-namespace:Content.Client.RPSX.Bank.PDA.UI.Cartridges"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               x:Class="Content.Client.RPSX.Bank.PDA.UI.Cartridges.HeadShopUiFragment"
                               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                               Margin="1 0 2 0"
                               Orientation="Vertical"
                               HorizontalExpand="True"
                               VerticalExpand="True">
    <PanelContainer StyleClasses="BackgroundDark"></PanelContainer>
    <BoxContainer Name="Bying" Orientation="Vertical" Margin="5 0 5 0">
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'cargo-console-menu-account-name-label'}"
                StyleClasses="LabelKeyText" />
            <Label Name="AccountNameLabel"
                Text="{Loc 'cargo-console-menu-account-name-none-text'}" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'cargo-console-menu-points-label'}"
                StyleClasses="LabelKeyText" />
            <Label Name="PointsLabel"
                Text="$0" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'cargo-console-menu-order-capacity-label'}"
                StyleClasses="LabelKeyText" />
            <Label Name="ShuttleCapacityLabel"
                Text="0/20" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <OptionButton Name="Categories"
                        Prefix="{Loc 'cargo-console-menu-categories-label'}"
                        HorizontalExpand="True" />
            <LineEdit Name="SearchBar"
                    PlaceHolder="{Loc 'cargo-console-menu-search-bar-placeholder'}"
                    HorizontalExpand="True" />
        </BoxContainer>
        <ScrollContainer HorizontalExpand="True"
                        VerticalExpand="True"
                        SizeFlagsStretchRatio="6">
            <BoxContainer Name="Products"
                        Orientation="Vertical"
                        HorizontalExpand="True"
                        VerticalExpand="True">
                <!-- Products get added here by code -->
            </BoxContainer>
        </ScrollContainer>
    </BoxContainer>
    <BoxContainer Name="Basket" Orientation="Vertical" Visible="False">
        <ScrollContainer HorizontalExpand="True"
                        VerticalExpand="True"
                        SizeFlagsStretchRatio="6">
            <BoxContainer Name="BuyedProducts"
                        Orientation="Vertical"
                        HorizontalExpand="True"
                        VerticalExpand="True">
                <!-- Products get added here by code -->
            </BoxContainer>
        </ScrollContainer>
        <Button Name="SubmitButton"
                Access="Public"
                Disabled="True"
                HorizontalAlignment="Right"
                Text="{Loc 'head-pda-order-menu-submit-button'}"
                TextAlign="Center" />
    </BoxContainer>
</cartridges:HeadShopUiFragment>
